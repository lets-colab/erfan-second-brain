PORT=8787
CLAUDE_DESIGN_MCP_URL=https://api.anthropic.com/v1/design/mcp
# Design-scoped OAuth access token. Configure only in a secret store / hosting env.
CLAUDE_DESIGN_ACCESS_TOKEN=
# Optional development gate for the bridge itself. Do not expose an unauthenticated bridge publicly.
BRIDGE_API_KEY=
